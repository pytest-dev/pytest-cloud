language: python
addons:
  firefox: "32.0.3"
# command to install dependencies
install:
  - pip install python-coveralls virtualenv
# # command to run tests
script: python setup.py test
after_success:
  - pip install -r requirements-testing.txt -e .
  - py.test --cov=pytest_cloud --cov-report=term-missing tests
  - coveralls
branches:
  except:
    - /^\d/
notifications:
    email:
        - bubenkoff@gmail.com
